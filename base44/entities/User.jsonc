{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "settings": {
      "type": "string",
      "description": "JSON-encoded user settings"
    },
    "blocked_users": {
      "type": "string",
      "description": "JSON-encoded list of blocked user ids"
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}