{
  "name": "RevenueCatPurchase",
  "type": "object",
  "description": "Provider-isolated ledger for RevenueCat / Google Play purchases. Do not reuse Base44Purchase (Wix-only).",
  "properties": {
    "event_id": {
      "type": "string",
      "description": "RevenueCat event id for the latest processed event on this row. Used for event-level idempotency."
    },
    "event_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "All RevenueCat event ids already applied to this transaction (duplicate delivery safety)."
    },
    "user_id": {
      "type": "string",
      "description": "Base44 user id resolved from RevenueCat app_user_id."
    },
    "app_user_id": {
      "type": "string",
      "description": "RevenueCat app_user_id at event time."
    },
    "original_app_user_id": {
      "type": "string",
      "description": "RevenueCat original_app_user_id when present."
    },
    "product_id": {
      "type": "string",
      "description": "Store product id, e.g. trailblazer.30month"
    },
    "store": {
      "type": "string",
      "description": "RevenueCat store, e.g. PLAY_STORE"
    },
    "environment": {
      "type": "string",
      "description": "SANDBOX or PRODUCTION"
    },
    "transaction_id": {
      "type": "string",
      "description": "Store transaction id. Primary idempotency key with product_id."
    },
    "original_transaction_id": {
      "type": "string",
      "description": "Original store transaction id when distinct from transaction_id."
    },
    "event_type": {
      "type": "string",
      "description": "Latest RevenueCat event type applied to this row"
    },
    "purchase_date": {
      "type": "string",
      "format": "date-time",
      "description": "Purchase timestamp from RevenueCat purchased_at_ms"
    },
    "purchased_at": {
      "type": "string",
      "format": "date-time",
      "description": "Purchase timestamp alias for integrations that use purchased_at."
    },
    "plan_expiration_date": {
      "type": "string",
      "format": "date-time",
      "description": "App-owned expiry: purchase_date + 30 calendar months (UTC clamp)"
    },
    "expiration_at": {
      "type": "string",
      "format": "date-time",
      "description": "Expiry timestamp alias for integrations that use expiration_at."
    },
    "product_name": {
      "type": "string",
      "description": "Display name for purchase history"
    },
    "amount": {
      "type": "number",
      "description": "Price amount from RevenueCat event (absolute)"
    },
    "price": {
      "type": "number",
      "description": "Price paid in the purchased currency."
    },
    "currency": {
      "type": "string",
      "description": "ISO currency code"
    },
    "status": {
      "type": "string",
      "enum": [
        "paid",
        "active",
        "refunded",
        "expired"
      ],
      "default": "paid"
    }
  },
  "required": [
    "event_id",
    "user_id",
    "app_user_id",
    "product_id",
    "transaction_id",
    "status"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}